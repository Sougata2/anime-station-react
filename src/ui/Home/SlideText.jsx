import styled from "styled-components";

const StyledSlideText = styled.div``;
function SlideText({ name, description, episodes }) {
  return <StyledSlideText></StyledSlideText>;
}

export default SlideText;
